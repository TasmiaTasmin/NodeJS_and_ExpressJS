/*
function a(){
    console.log(b);
    
    sum();

    function sum(){
        console.log(b);
    }
}

var b = 30;

console.log(b);

a();
*/

/*
*/



/*
    * Topics:
        1. Pre increment, post increment
        2. Scope: If I declare a variable, then from where I can access the function.
				  If any function can access a variable, then the variable has the scope in that function.
				  Scope is also called as Lexical Scope.
        3. Scope Chain: Scope Chain is also called as Lexical Scope Chain.
        4. Lexical Environment: memory component of an execution context + lexical environment of it's parent.
*/

